# secrets
.env

# local-only overrides (e.g. port conflicts specific to your machine —
# see README "Lessons from running this on real hardware")
docker-compose.override.yml
.env.k8s

# node
node_modules/
dist/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.venv/
venv/
.fastembed_cache/

# generated (regenerate anytime with: buf generate / grpc_tools.protoc —
# see README "Regenerating proto code". Committed here for zero-friction
# clone-and-run, but treat this dir as build output, not source of truth.)
# gateway/src/gen/
# supervisor/gen/

# OS
.DS_Store
*_debug.txt *.log
